const state = () => ({
    // count: 0
})

const mutations  = {
    // getCount: (state,count) => {
    //     state.count = count
    // }
}

const actions = {
    // setCount: ({commit},count) => {
    //     commit('getCount', count)
    // }
}

export default {
    state,
    mutations,
    actions
}